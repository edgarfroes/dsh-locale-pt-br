/**
 * Host half of the Brazilian Portuguese language pack (dsh-locale-pt-br).
 *
 * The pack only contributes browser UI copy, so the Host registers nothing.
 * The browser half (lib/client.js) is found by the client module scan because
 * package.json declares `dsh.client`.
 */

export function apply() {}
